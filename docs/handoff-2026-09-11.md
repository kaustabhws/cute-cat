# Companion milestone handoff — 11 September 2026

## Scope and behavior

CAT-01–06, NOT-01–03, FOC-01/02, local SUM controls and FREE-01. Rebuilt the missing native code/assets. Implemented the user's requested walking, running, playing, grooming, sleeping/waking and notification paw dismissal with original art. Kept the UI a quiet native utility and made the entire app free.

## Files changed

- `src/CuteCat.Core`: deterministic motion, acceleration/gait timing, target identity policy, focus sessions and atomic storage.
- `src/CuteCat.App`: WPF screens/themes, Win32 cat surface, bounded art cache, scoped notification worker, practice card, tray, settings and native QA modes.
- `assets`: original cat frame pack, v2 manifest and Windows icon.
- `art-source`, `output/imagegen`: prompts, untouched Azure outputs, cleaned source sheets and provenance. No key values.
- `scripts`: build/publish preflight, art export, offline in-betweens, validation and XPS rendering; `tests/CuteCat.Tests`: 43 meaningful deterministic checks.
- `docs`: corrected the missing-code status, refreshed primary sources, recorded free/native scope, added notification and motion contracts, and updated current requirements and agent context.
- `CuteCat.slnx`, `global.json`, `.gitignore`, `README.md`, `Start Cute Cat.cmd`: reproducible local entry points.

## Checks actually run

`dotnet run --project tests/CuteCat.Tests -c Release`; self-contained Release publish through `scripts/build.ps1`; native executable `--smoke-test` in isolated state folders; `--render-previews` followed by XPS rasterization; `scripts/validate_project.py`; short `--benchmark` in tray mode. Core: 43/43. Native: 14/14 scenarios, successful layered compositing, contact within 0.44px using actual HWND bounds and the correct rendered paw frame before dismissal. The user separately confirmed real Windows banner dismissal.

Review corrected magenta residue and grid lines, foot registration, ghosted gallop in-betweens, inherited button text contrast, missing dark export background, Windows motion fallback silently disabling the action tests, and theme selector state in exports. Final assets retain the same reference and anatomical markings.

## Evidence, limitations and next work

Use `docs/build-status.md` and the artifacts it lists. The native screenshot/input helper could read accessibility but could not capture the monitor or move/click the cursor; XPS rendering and routed events are identified honestly. Real banner support is user-confirmed on this machine, not guaranteed across all Windows versions. Physical click-through, mixed-monitor/DPI and broader accessibility QA remain the next desktop proof gates. The next separate feature milestone is M05 browser reconstruction if requested; its existing contracts are proposals, not a shipped extension.

Decisions D11–D17 cover the documentation-only starting state, free pricing, native appearance, narrow notification authorization, v002 reference, motion preferences and the v2 asset contract. No publishing or external distribution occurred.
